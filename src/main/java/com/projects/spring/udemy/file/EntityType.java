package com.projects.spring.udemy.file;

/**
 * enum used by methods for uploading and getting files
 * to distinguish between courses, comments and authors
 */
public enum EntityType {
    COURSE, COMMENT, AUTHOR
}
